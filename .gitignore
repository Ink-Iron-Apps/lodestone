.venv/
__pycache__/
*.pyc
.pytest_cache/
.env

# Fixtures are snapshots of real pages; the HTML itself is FFN's, so it stays
# local. Regenerate with scripts/save_fixtures.py.
crawler/tests/fixtures/*.html
